<Page x:Class="FMartFUDAApp.CustomerHistoryPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FMartFUDAApp"
        mc:Ignorable="d"
        Title="CustomerHistoryPage" Height="450" Width="800">
    <Grid Margin="10">
        <TextBlock Text="LỊCH SỬ KHÁCH HÀNG"
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10" />

        <DataGrid x:Name="dgCustomerHistory"
                  Margin="0,40,0,0"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding HistoryId}" Width="50"/>
                <DataGridTextColumn Header="Loại thao tác" Binding="{Binding ActionType}" Width="100"/>
                <DataGridTextColumn Header="Thời gian" Binding="{Binding ActionDate}" Width="150"/>
                <DataGridTextColumn Header="Nhân viên" Binding="{Binding EmployeeId}" Width="80"/>
                <DataGridTextColumn Header="Mô tả" Binding="{Binding ChangeDecription}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
