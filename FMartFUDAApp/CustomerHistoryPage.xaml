<Page x:Class="FMartFUDAApp.CustomerHistoryPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FMartFUDAApp"
        mc:Ignorable="d"
      Title="Customer History" Background="White">
        <Grid Margin="0">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- Tiêu đề -->
    <TextBlock Text="LỊCH SỬ KHÁCH HÀNG"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="10"/>

    <!-- DataGrid hiển thị lịch sử -->
    <DataGrid x:Name="dgCustomerHistory"
                  Grid.Row="1"
                  Margin="10"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch">
        <DataGrid.Columns>
            <DataGridTextColumn Header="ID" Binding="{Binding HistoryId}" Width="Auto"/>
            <DataGridTextColumn Header="Loại thao tác" Binding="{Binding ActionType}" Width="*"/>
            <DataGridTextColumn Header="Thời gian" Binding="{Binding ActionDate, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
            <!-- Sử dụng binding đến Employee.EmployeeName -->
            <DataGridTextColumn Header="Nhân viên" Binding="{Binding EmployeeId}" Width="*"/>
            <DataGridTextColumn Header="Mô tả" Binding="{Binding ChangeDecription}" Width="2*"/>
        </DataGrid.Columns>
    </DataGrid>
    </Grid>
</Page>
